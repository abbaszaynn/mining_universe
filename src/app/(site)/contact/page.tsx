import { ComingSoonPanel } from "@/components/layout/PageShell";

export default function ContactPage() {
  return (
    <ComingSoonPanel
      title="Contact & investor desk"
      description="Inquiry forms and direct investor contacts — backend integration comes last."
    />
  );
}
